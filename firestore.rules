
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ DEVELOPMENT MODE: Allow all reads and writes
    // 배포 전에는 반드시 보안 규칙을 다시 강화해야 합니다.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
